{
  "name": "@harvest/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Harvest — application desktop (Tauri) enveloppant l'application web",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
